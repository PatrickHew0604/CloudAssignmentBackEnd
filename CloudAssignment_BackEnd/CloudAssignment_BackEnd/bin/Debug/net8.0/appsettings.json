{
    "Logging": {
        "LogLevel": {
            "Default": "Information",
            "Microsoft.AspNetCore": "Warning"
        }
    },
    "AllowedHosts": "*",
    "ConnectionStrings": {
        "EConsultationDBAuthConnectionString": "Server=PATRICK;Database=EConsultationAuthDB;Trusted_Connection=True;Trusted_Connection=True;TrustServerCertificate=True"
    },
    "Jwt": {
        "Key": "This is a secret key for authentication",
        // Issuer will need to change to my backend URL after deployment
        "Issuer": "https://localhost:32771",
        // Audience will need to change to my frontend URL after deployment
        "Audience": "https://localhost:32771"
    }
}
